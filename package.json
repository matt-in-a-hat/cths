{
  "name": "CTHS",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build-css": "node-sass --include-path scss themes/cths/scss/main.scss public/css/cths.css --source-map-embed true",
    "watch-css": "nodemon -e scss -x \"npm run build-css\"",
    "build:cms": "docker-compose exec web ./vendor/silverstripe/framework/sake dev/build flush=1",
    "composer:install": "docker-compose exec web composer install --no-dev",
    "composer:update": "docker-compose exec web composer update"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.5.2",
    "nodemon": "^1.12.1"
  }
}
